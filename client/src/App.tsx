import { TableauStock } from "@/components/TableauStock";

export default function Dashboard() {
  return (
    <div className="container mx-auto p-6 max-w-7xl space-y-6">
      <TableauStock />
    </div>
  );
}